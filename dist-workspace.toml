[workspace]
members = ["cargo:."]

# Config for 'dist'
#
# OPEN ITEM before the first tag: dist builds one app (and one formula) per
# binary package, so today this would publish portman-cli.rb + portman-daemon.rb.
# Ship ONE `portman` formula with both binaries by making the daemon a lib
# crate with its bin target living in a single distributable package.
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.32.0"
# CI backends to support
ci = "github"
# The installers to generate for each app
installers = ["shell", "homebrew"]
# Target platforms to build apps for (Rust target-triple syntax)
targets = ["aarch64-apple-darwin", "aarch64-unknown-linux-gnu", "x86_64-apple-darwin", "x86_64-unknown-linux-gnu"]
# Path that installers should place binaries in
install-path = "CARGO_HOME"
# Whether to install an updater program
install-updater = false
# Publish the formula into the existing personal tap (Formula/portman*.rb).
# NOTE: publishing only happens on a tagged release — do not tag until the
# portman repo itself is public, or the formula will point at private assets.
tap = "MortenHusted/homebrew-tap"
publish-jobs = ["homebrew"]
