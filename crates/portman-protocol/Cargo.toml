[package]
name = "portman-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "IPC protocol types shared between portman daemon and clients"

[dependencies]
serde.workspace = true
serde_json.workspace = true
anyhow = { workspace = true, optional = true }
tokio = { workspace = true, optional = true }

[features]
## Length-prefixed JSON framing helpers (pulls in tokio).
transport = ["dep:anyhow", "dep:tokio"]

[lints]
workspace = true
